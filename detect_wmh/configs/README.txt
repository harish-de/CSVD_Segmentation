the tunable parameters are changed using json file "config.json"

json config includes:
1. training parameters:
    -   optimizer:
    -   learning rate:
    -   epochs:

2. data loader:
    -   batch size:
    -   validation set: